import path from 'path'

const PREFIX = '!'
const BOT_EMOJI = '🥭'
const BOT_NAME = 'Zé Da Manga'
const TEMP_FOLDER = path.resolve(__dirname, '..', 'auth', 'temp')

export { BOT_EMOJI, PREFIX, TEMP_FOLDER, BOT_NAME }